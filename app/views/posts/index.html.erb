<div class="contents row">
  <% @posts.each do |post| %>
    <div class="content_post" style="background-image: url(<%= url_for(post.image) %>);">
      <p><%= post.explanation %></p>
      <span class="name">
        <%= post.user.nickname %>
      </span>
    </div>
  <% end %>
</div>